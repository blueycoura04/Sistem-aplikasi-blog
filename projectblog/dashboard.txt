<?php
session_start();
include "../koneksi.php";

/* CEK LOGIN */
if(!isset($_SESSION['login'])){
    header("Location: ../login.php");
    exit;
}

/* CEK ROLE PENULIS */
if($_SESSION['role'] != 'penulis'){
    header("Location: ../login.php");
    exit;
}

$username = $_SESSION['username'];

/* HITUNG DATA ARTIKEL MILIK PENULIS */
$query_total = mysqli_query($conn, "
SELECT * FROM artikel WHERE username='$username'
");
$total_artikel = mysqli_num_rows($query_total);

/* ARTIKEL TERBARU */
$artikel = mysqli_query($conn, "
SELECT * FROM artikel 
WHERE username='$username' 
ORDER BY tanggal DESC 
LIMIT 5
");
?>
<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<title>Dashboard Penulis</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<style>
body {
    background: linear-gradient(135deg, #4facfe, #00f2fe);
    min-height: 100vh;
    font-family: 'Segoe UI', sans-serif;
}

.navbar {
    background: #002366;
}

.card {
    border-radius: 15px;
}

.card-blue {
    background: linear-gradient(135deg,#4facfe,#00f2fe);
    color: white;
}

.table thead {
    background: #002366;
    color: white;
}
</style>
</head>
<body>

<!-- NAVBAR -->
<nav class="navbar navbar-dark">
    <div class="container">
        <span class="navbar-brand">Dashboard Penulis</span>
        <a href="../logout.php" class="btn btn-light">Logout</a>
    </div>
</nav>

<div class="container mt-4">

    <!-- WELCOME -->
    <h3>Halo, <b><?= $_SESSION['username']; ?></b> 👋</h3>

    <!-- CARD -->
    <div class="row mt-4">
        <div class="col-md-4">
            <div class="card card-blue shadow text-center">
                <div class="card-body">
                    <h5>Total Artikel</h5>
                    <h2><?= $total_artikel; ?></h2>
                </div>
            </div>
        </div>
    </div>

    <!-- MENU -->
    <div class="mt-4">
        <a href="artikel/index.php" class="btn btn-primary">Kelola Artikel</a>
        <a href="artikel/tambah.php" class="btn btn-success">+ Tambah Artikel</a>
    </div>

    <!-- ARTIKEL TERBARU -->
    <div class="mt-5">
        <h4>Artikel Terbaru Kamu</h4>

        <table class="table table-bordered mt-3">
            <thead>
                <tr>
                    <th>No</th>
                    <th>Judul</th>
                    <th>Tanggal</th>
                </tr>
            </thead>
            <tbody>
                <?php $no=1; while($row = mysqli_fetch_assoc($artikel)) { ?>
                <tr>
                    <td><?= $no++; ?></td>
                    <td><?= htmlspecialchars($row['judul']); ?></td>
                    <td><?= $row['tanggal']; ?></td>
                </tr>
                <?php } ?>
            </tbody>
        </table>

    </div>

</div>

</body>
</html>